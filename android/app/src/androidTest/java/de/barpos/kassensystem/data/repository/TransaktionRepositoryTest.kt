package de.barpos.kassensystem.data.repository

import org.junit.Test

class TransaktionRepositoryTest {

    @Test
    fun `full save - lock - storno cycle`() {
        // This test must fail first
        assert(false)
    }
}
