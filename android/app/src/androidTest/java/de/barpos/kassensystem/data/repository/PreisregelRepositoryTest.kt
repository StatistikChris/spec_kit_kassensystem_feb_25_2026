package de.barpos.kassensystem.data.repository

import org.junit.Test

class PreisregelRepositoryTest {

    @Test
    fun `CRUD operations work correctly`() {
        // This test must fail first
        assert(false)
    }

    @Test
    fun `active-rules-at-time query is correct`() {
        // This test must fail first
        assert(false)
    }
}
