package de.barpos.kassensystem.data.repository

import org.junit.Test

class SchichtTest {

    @Test
    fun `shift-close sets endZeit`() {
        // This test must fail first
        assert(false)
    }

    @Test
    fun `transactions are linked via bedienerid`() {
        // This test must fail first
        assert(false)
    }
}
