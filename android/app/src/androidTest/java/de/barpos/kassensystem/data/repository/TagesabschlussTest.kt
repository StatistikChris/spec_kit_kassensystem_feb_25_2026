package de.barpos.kassensystem.data.repository

import org.junit.Test

class TagesabschlussTest {

    @Test
    fun `locked flag set on all Transaktionen after Z-Bon`() {
        // This test must fail first
        assert(false)
    }

    @Test(expected = Exception::class)
    fun `write to locked throws exception`() {
        // This test must fail first
        assert(false)
    }
}
