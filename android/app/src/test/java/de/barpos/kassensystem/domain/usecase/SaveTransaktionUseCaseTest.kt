package de.barpos.kassensystem.domain.usecase

import org.junit.Test

class SaveTransaktionUseCaseTest {

    @Test
    fun `cash payment assembly is correct`() {
        // This test must fail first
        assert(false)
    }

    @Test
    fun `EC payment assembly is correct`() {
        // This test must fail first
        assert(false)
    }

    @Test
    fun `positions are included`() {
        // This test must fail first
        assert(false)
    }

    @Test
    fun `TSE sign is triggered`() {
        // This test must fail first
        assert(false)
    }

    @Test
    fun `total in cents is correct`() {
        // This test must fail first
        assert(false)
    }
}
