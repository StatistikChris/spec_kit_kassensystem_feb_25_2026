package de.barpos.kassensystem.domain.usecase

import org.junit.Test

class BedienerAuthUseCaseTest {

    @Test
    fun `correct PIN hash is accepted`() {
        // This test must fail first
        assert(false)
    }

    @Test
    fun `wrong PIN is rejected`() {
        // This test must fail first
        assert(false)
    }

    @Test
    fun `trainee flag is correctly identified`() {
        // This test must fail first
        assert(false)
    }
}
