package de.barpos.kassensystem.ui.varianten

import org.junit.Test

class VariantPickerViewModelTest {

    @Test
    fun `single-variant auto-selects`() {
        // This test must fail first
        assert(false)
    }

    @Test
    fun `multi-variant shows bottom sheet`() {
        // This test must fail first
        assert(false)
    }
}
