package de.barpos.kassensystem.ui.kasse

import org.junit.Test

class TransaktionViewModelTest {

    @Test
    fun `add position updates totals`() {
        // This test must fail first
        assert(false)
    }

    @Test
    fun `remove position updates totals`() {
        // This test must fail first
        assert(false)
    }

    @Test
    fun `vat split is calculated correctly`() {
        // This test must fail first
        assert(false)
    }
}
