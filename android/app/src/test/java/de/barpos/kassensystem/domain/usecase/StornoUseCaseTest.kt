package de.barpos.kassensystem.domain.usecase

import org.junit.Test

class StornoUseCaseTest {

    @Test
    fun `TSE cancel sign called`() {
        // This test must fail first
        assert(false)
    }

    @Test
    fun `linked StornoTransaktion persisted`() {
        // This test must fail first
        assert(false)
    }

    @Test(expected = StornoNachBonException::class)
    fun `throws StornoNachBonException when Bon exists for transaktionId`() {
        // This test must fail first
        assert(false)
    }
}
