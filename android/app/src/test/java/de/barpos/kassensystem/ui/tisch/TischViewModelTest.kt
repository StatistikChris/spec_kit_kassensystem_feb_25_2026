package de.barpos.kassensystem.ui.tisch

import org.junit.Test

class TischViewModelTest {

    @Test
    fun `open table changes status`() {
        // This test must fail first
        assert(false)
    }

    @Test
    fun `split table creates new table`() {
        // This test must fail first
        assert(false)
    }

    @Test
    fun `merge tables combines transactions`() {
        // This test must fail first
        assert(false)
    }

    @Test
    fun `optimistic lock conflict is handled`() {
        // This test must fail first
        assert(false)
    }
}
