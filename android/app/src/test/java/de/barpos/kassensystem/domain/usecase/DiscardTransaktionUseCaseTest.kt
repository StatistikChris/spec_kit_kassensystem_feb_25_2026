package de.barpos.kassensystem.domain.usecase

import org.junit.Test

class DiscardTransaktionUseCaseTest {

    @Test
    fun `in-progress discard leaves zero Room records`() {
        // This test must fail first
        assert(false)
    }
}
