package de.barpos.kassensystem.domain.usecase

import org.junit.Test

class GenerateZBonUseCaseTest {

    @Test
    fun `Z-Bon aggregation is correct`() {
        // This test must fail first
        assert(false)
    }

    @Test
    fun `locked flag is set on all included Transaktionen`() {
        // This test must fail first
        assert(false)
    }
}
